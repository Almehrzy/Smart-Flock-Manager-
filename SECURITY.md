# سياسة الأمان | Security Policy

## الإبلاغ عن الثغرات | Reporting Security Vulnerabilities

إذا اكتشفت ثغرة أمنية، **يرجى عدم فتح Issue عام**. بدلاً من ذلك:

1. **لا تشارك الثغرة علناً** | Do not share vulnerabilities publicly
2. **أرسل بريد إلكتروني** إلى maintainers مباشرة
3. **أرفق تفاصيل**:
   - وصف الثغرة
   - خطوات الاستنساخ
   - التأثير المحتمل
   - الحل المقترح (إن أمكن)

---

## 🔐 معايير الأمان | Security Standards

### متغيرات البيئة | Environment Variables
- ✅ استخدم `.env.local` للتطوير
- ✅ لا تشارك مفاتيح API عبر Git
- ✅ استخدم متغيرات البيئة الآمنة في الإنتاج
- ❌ لا تضع مفاتيح في الكود

### قاعدة البيانات | Database
- ✅ استخدم Row Level Security (RLS) في Supabase
- ✅ تحقق من صلاحيات المستخدمين
- ✅ تشفير البيانات الحساسة
- ❌ لا تخزن كلمات المرور نصية

### المصادقة | Authentication
- ✅ استخدم Supabase Auth أو نظام آمن
- ✅ تحقق من هوية المستخدمين
- ✅ استخدم JWT Tokens
- ❌ لا تخزن بيانات حساسة في localStorage

### API الخارجية | External APIs
- ✅ تحقق من صحة البيانات الواردة
- ✅ استخدم HTTPS فقط
- ✅ محدود معدل الطلبات (Rate Limiting)
- ❌ لا تثق بالبيانات الخارجية مباشرة

---

## 🛡️ أفضل الممارسات | Best Practices

### تطوير آمن | Secure Development
```typescript
// ✓ صحيح - تحقق من صحة البيانات
if (!user || user.role !== 'Admin') {
  throw new Error('Unauthorized');
}

// ✗ خطأ - ثقة عمياء
const data = await supabase.from('users').select();
```

### الأطقمة الأمنية | Security Headers
- استخدم HTTPS في الإنتاج
- أضف Content Security Policy (CSP)
- استخدم CORS بشكل صحيح

### التحديثات | Updates
- حدّث المكتبات بانتظام
- فعّل `npm audit` و `npm audit fix`
- راقب الثغرات الأمنية

---

## 📋 قائمة التحقق | Security Checklist

قبل الإطلاق على الإنتاج:

- [ ] جميع مفاتيح API آمنة في متغيرات البيئة
- [ ] `.env.local` مضافة في `.gitignore`
- [ ] تم اختبار نظام المصادقة
- [ ] تم تفعيل RLS في Supabase
- [ ] تم التحقق من صحة جميع المدخلات
- [ ] تم تحديث جميع المكتبات
- [ ] لا توجد ثغرات معروفة (`npm audit`)

---

## 📞 التواصل | Contact

للمشاكل الأمنية الحساسة، تواصل معنا بحذر وسرية.

**شكراً على مساعدتك في الحفاظ على أمان المشروع!** 🛡️
