# المساهمة في Smart Flock Manager | Contributing to Smart Flock Manager

شكراً لاهتمامك بالمساهمة! | Thank you for your interest in contributing!

## 📋 قبل البدء | Before You Start

- اقرأ [README.md](./README.md)
- تأكد من فهمك لبنية المشروع
- اتبع معايير الكود الخاصة بنا

## 🔄 خطوات المساهمة | Contribution Steps

### 1. إنشاء Fork
```bash
git clone https://github.com/your-username/smart-flock-manager-3.0.git
cd smart-flock-manager-3.0
```

### 2. إنشاء فرع جديد
```bash
git checkout -b feature/your-feature-name
# أو للإصلاحات:
git checkout -b fix/your-bug-fix
```

### 3. عمل التغييرات
- اتبع نمط الكود الموجود
- استخدم TypeScript
- أضف تعليقات واضحة
- اختبر التغييرات محلياً

### 4. الالتزام بالتغييرات
```bash
git add .
git commit -m "نوع: وصف واضح للتغيير"
# أمثلة:
# git commit -m "feat: إضافة صفحة جديدة"
# git commit -m "fix: إصلاح خطأ في المصادقة"
# git commit -m "docs: تحديث التوثيق"
```

### 5. فتح Pull Request
- اشرح التغييرات بوضوح
- أرفق لقطات شاشة إن أمكن
- اذكر أي issues ذات صلة

## ✅ معايير الكود | Code Standards

### TypeScript
```typescript
// استخدم أنواع واضحة
interface User {
  id: string;
  name: string;
  role: UserRole;
}

// تجنب any
❌ const user: any = {}; // لا تفعل هذا
✅ const user: User = {}; // افعل هذا
```

### التسمية | Naming
- Components: `PascalCase` (e.g., `UserCard.tsx`)
- Functions/Variables: `camelCase` (e.g., `getUserData`)
- Constants: `UPPER_SNAKE_CASE` (e.g., `MAX_ITEMS`)

### التعليقات | Comments
```typescript
// استخدم تعليقات واضحة وموجزة
// ✓ Good
// Calculate total income from profit share
const totalIncome = employees.reduce(...);

// ✗ Avoid
// loop through employees
const totalIncome = employees.reduce(...);
```

## 🧪 الاختبار | Testing

قبل فتح Pull Request:
```bash
npm run build    # تأكد من عدم وجود أخطاء
npm run dev      # اختبر محلياً
```

## 📝 أنواع الرسائل | Commit Message Types

- **feat**: ميزة جديدة
- **fix**: إصلاح خطأ
- **docs**: تحديثات التوثيق
- **style**: تنسيق الكود (بدون تغيير منطقي)
- **refactor**: إعادة هيكلة الكود
- **perf**: تحسينات الأداء
- **test**: إضافة أو تحديث الاختبارات

## 🚫 قواعم مهمة | Important Rules

- ❌ **لا تشارك مفاتيح API** أو البيانات السرية
- ❌ **لا تحذف ملفات** قد تكون مهمة للآخرين
- ✅ **اختبر تغييراتك** قبل الرفع
- ✅ **اتبع المعايير الموجودة** في المشروع
- ✅ **اكتب رسائل واضحة** عن التغييرات

## 🐛 الإبلاغ عن الأخطاء | Reporting Bugs

عند فتح Issue:
1. اشرح المشكلة بوضوح
2. أرفق خطوات الاستنساخ
3. اذكر النتيجة المتوقعة والفعلية
4. أرفق لقطات شاشة/سجلات إن أمكن

## 💡 الميزات الجديدة | Feature Requests

عند اقتراح ميزة:
1. اشرح الحالة الاستخدام
2. اشرح الفائدة المتوقعة
3. وفر أمثلة إن أمكن

---

**شكراً على المساهمة!** 🙏

**Thank you for contributing!** 🙏
